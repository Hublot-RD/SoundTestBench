#ifndef MOTOR_H
#define MOTOR_H

#include <Arduino.h>

namespace motor {
    // Constants
    // constexpr uint8_t NUM_LEDS = 8;         // Number of LEDs

    // Pinout
    // constexpr uint8_t LEDS_PIN = 20;          // Pins for the 8 LEDs

    // Function prototypes
    void setup(void);
    
} // namespace motor

#endif // MOTOR_H